a = [3, 10, 765, 203, 13, 900, 1000]
 
def b_sort(array):
    lenght = len(array)
    for i in range(lenght):
        for j in range(0, lenght-i-1):
            if array[j]>array[j+1]:
                temp = array[j]
                array[j] = array[j+1]
                array[j+1] = temp
b_sort(a)
print(a)
#sorted() и sort(). Эти функции отлажены и работают быстро и эффективно.

a.sort()
print(a)
                
